{
  "name": "github-project-agent",
  "main": "agent.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:core": "vitest run tests/contract.spec.ts tests/scenario.spec.ts",
    "test:e2e": "vitest run tests/e2e_nl_sql.spec.ts",
    "start:slack": "bun slack.ts",
    "format": "prettier --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ai": "^5.0.44",
    "blink": "^0.1.19",
    "esbuild": "^0.25.9",
    "pg": "^8.13.0",
    "zod": "^4.1.8",
    "@slack/bolt": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "@types/pg": "^8.15.5",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.0.0"
  }
}
